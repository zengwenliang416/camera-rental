# Camera Rental — 项目协作入口

本文件保存项目共同约束和按需阅读入口。具体实现以当前源码、清单、迁移和测试为证据；
应用目录的上游 README、历史计划和验收记录不能证明当前已实现或已部署。

## 1. 项目地图与职责

当前是**单个 Git 仓库下的多个应用目录**，不是 Git 子模块或多个独立仓库。
分支、HEAD、远程和工作区状态在任务开始时读取，不把上游分支名写成当前基线。

| 目录 / 指令入口 | 职责 | 技术与包管理 |
| --- | --- | --- |
| [server](camera-rental-server/AGENTS.md) | 权限、租赁领域、数据库、排期、金额、第三方集成 | Java 17、Spring Boot 3、MyBatis Plus、Maven |
| [admin](camera-rental-admin/AGENTS.md) | Web 管理后台、设备、订单、配置与报表 | Vue 3、TypeScript、Element Plus、pnpm |
| [schedule-center](camera-rental-schedule-center/AGENTS.md) | 独立排期与履约工作台，复用管理端认证与权限 | React、TypeScript、Vite、pnpm |
| [uniapp](camera-rental-uniapp/AGENTS.md) | 客户商城、小程序、H5、App | Vue 3、uni-app、pnpm 锁文件 |
| [staff](camera-rental-staff/AGENTS.md) | 员工扫码、出库、回仓、检测和维修 | Vue 3、TypeScript、uni-app、Wot UI、pnpm |
| [web](camera-rental-web/AGENTS.md) | PC 客户官网、客户归还入口与 SEO | Nuxt、Vue 3、bun |
| [docs](docs/README.md) | 跨应用架构、领域、接口、工程规则与决策 | 按任务读取 |
| `openspec/` | 变更提案、合同、任务与验证证据 | 只读取与本任务匹配的变更 |

版本与可运行命令以目标应用的 `package.json`、锁文件、`pom.xml` 为准。
存在源码模块不等于已装配启用；后端同时检查根 POM 与 `yudao-server/pom.xml`。

## 2. 渐进式披露：先入口，再任务所需细节

1. 应用从全局到目标目录的指令链；同层 `AGENTS.override.md` 替代 `AGENTS.md`。
   只读目标路径上的更深层规则，不遍历所有无关应用。
2. 建立最小上下文：目标应用指令、README 的相关部分、清单与锁文件头、
   当前分支/HEAD、staged/unstaged/untracked 状态；锁文件只深入目标依赖。
3. 按下表加载本次所需文档，再定位现有入口、调用方、实现与测试。
   文档引用是按需入口，不要求递归读取全部链接或整个 `docs/`、`openspec/changes/`。
4. 实施前明确用户结果、涉及应用/API/数据表、复用点及验收方式。
   若有匹配的活动合同或任务，核对其范围与当前证据；不得擅改 gate/override 来绕过检查。
5. 简单改动保持轻量；出现契约、安全、数据或架构差异时再读取下一层证据。
   缺少关键事实时说明缺口，不以旧文档中的“计划”补作已完成事实。

| 任务 | 需要读取的规则 |
| --- | --- |
| 新增能力、抽取复用、重构、代码审查 | [工程规范](docs/engineering/standards.md)中的相关章节 |
| 前后端边界、跨应用功能、模块依赖 | [系统架构](docs/architecture.md)与[API 约定](docs/api/README.md) |
| 订单、费用、退款、押金 | [租赁订单](docs/domain/rental-order.md) |
| 排期、设备分配、归还、检测 | [设备排期](docs/domain/device-scheduling.md)；客户归还再读[归还登记](docs/domain/customer-return-registration.md) |
| 闲管家/闲鱼功能 | [集成开发规则](docs/engineering/xianyu.md)、[领域规则](docs/domain/xianyu-integration.md)、[官方来源](docs/integrations/xianyu/source.md)及当前目标接口在线文档 |
| 测试、构建、交付 | [验证规则与命令入口](docs/engineering/validation.md) |
| 发布、部署、生产排障 | `docs/deployment/` 内与目标环境匹配的文档和实际配置；授权单独确认 |

若 `.codegraph/` 可用且与 checkout 同步，优先用 CodeGraph 定位符号及影响范围，
再按需核对源码、Git 与测试；缺失、不可用或过期时用 `rg` 等原生工具，不自动重建索引。

## 3. 代码复用与架构底线

- 先查同业务服务、共享组件、请求封装、工具与依赖，再决定复用、组合、扩展或新增。
  新增共享能力时说明现有候选、为什么不适用、实际调用方和测试范围。
- 同一业务规则只有一个权威实现。库存、排期、租金、订单状态及权限由后端决定；
  各前端复用 API 契约，不能各写一套权威计算或状态判定。
- 先局部组合，再在真实重复且语义相同处抽取。通用层不能反向依赖页面或渠道业务。
  不为单一调用方建立万能工具、基类、配置引擎或新的共享包。
- 跨 Vue、React、uni-app 的复用优先是契约和稳定纯逻辑；不直接互相导入应用内部组件、
  store、认证缓存实现，也不通过复制页面实现复用。共享包需证明消费者、平台兼容与维护边界。
- 保持模块化单体与各端现有目录约定。设计模式用于解决真实变化点，优先组合；
  不以“DDD/SOLID/设计模式”为理由无关拆层、建微服务或替换框架。
- 跨端功能先核对后端契约，再处理所有受影响调用端；破坏性接口变化须先说明影响、
  迁移和回滚方案并获得授权，不能静默增加永久兼容层。

## 4. 不可破坏的业务与安全约束

- 区分商品 SKU、设备实例、租赁订单/明细、设备分配、渠道映射、计租周期和设备占用周期。
  ERP 数量库存不能代替单台设备生命周期；租赁业务在已有 `yudao-module-rental` 内演进。
- 同设备有效占用不能重叠；检查与分配在事务内完成，并有数据库级并发控制。
  取消、退款、换机、续租和提前归还按现有业务流程同步调整占用，不把这些要求当成已实现声明。
- 业务日期按 `Asia/Shanghai`；遵循领域文档的闭区间展示与半开区间存储契约。
  不用字符串或手写天数计算改变日期语义。
- 金额内部使用整数分或现有精确金额类型，禁止浮点结算。前端仅展示；
  闲鱼 `pay_amount` 是总租金收入来源，备注缺失不影响收入计入，退款、押金和赔偿分别统计。
- Controller 不直接访问 Mapper，数据库实体不直接作为响应；使用统一返回、分页、VO、
  转换、错误码与校验。管理/客户入口分开，租赁 API 分别为 `/admin-api/rental/**`、
  `/app-api/rental/**`；认证、文件等公共能力复用对应已有模块 API。
- 后端校验权限、租户和对象归属，前端按钮/路由限制不能替代授权；回调验签并保证幂等。
- SQL 结构变更提供增量迁移，不改已执行历史迁移；使用适当唯一约束/索引，保留逻辑删除
  与审计，不通过删除历史订单修复同步。
- 第三方写操作、生产写入及破坏性操作须有明确授权、目标核对和验证/回滚边界。
  闲管家默认只读，自动化测试不得操作真实店铺。
- 闲管家商家配置按租户由管理端维护于 `xianyu_application`，AppSecret 加密且不回显；
  不恢复 `XGJ_*` 环境变量兼容入口，字段加密主密钥仍由基础设施注入。
- 密钥、签名、完整请求头、客户手机号/地址/身份证/支付凭证不得进入 Git、普通日志、
  截图或测试数据；看板和普通导出订单号默认脱敏。禁止 Cookie/抓包等非正式闲鱼访问。
  本地敏感配置使用不提交文件或批准的凭据机制，示例只放占位值，不提交真实生产地址。

## 5. 变更与交付

- 调查、规划和 review 默认只读；实现请求直接完成范围内的可逆本地修改与验证。
  保留所有无关 dirty work；不得 reset、clean、覆盖或全仓格式化。
- 未经明确授权不 commit、push、创建 PR、删除已有分支、部署、发布或归档。
  本地实现通过检查不等于获得这些后续步骤的授权。
- 依赖沿用应用当前清单与锁文件；新增前检查已有能力，不混用包管理器，不擅自升级。
- 按[验证规则](docs/engineering/validation.md)完成相关检查和最终 diff review。
  行为、契约或架构变化同步更新对应文档；稳定约束放 AGENTS，详细规范放 docs，
  当前任务证据放对应变更材料，不把历史状态堆入本入口。
- 最终说明：修改的应用/文件与行为、实际执行的验证、未执行项及原因、迁移/配置/
  兼容性风险、尚待确认的假设。只报告当前证据，不把编译成功写成业务或部署验收通过。
