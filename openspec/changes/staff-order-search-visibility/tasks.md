# 手机订单可见性与状态栏修复

基线：dca553d1，手机公开版本 1.1.0。本次为用户授权的本地修复，生产数据保持不变。

- [x] 保留综合搜索任一字段匹配，不纠正或近似合并输入错误的手机号。
- [x] 新增只读渠道待处理查询，与内部租赁订单独立分页，避免 ID 混用或查询时触发建单。
- [x] 已有内部单返回准备状态/原因，手机显示日期矛盾、缺日期、型号待配置等提示。
- [x] 区分无结果与渠道查询失败；切换条件/离开页面后丢弃过期请求。
- [x] 将 7 个相关页面的状态栏留白移到滚动区域外，补充原生高度与回前台零值保护。
- [x] 验证备注变化重新拉取详情、未变详情仍重评受阻订单；保留日期规则和已履约保护。
- [x] 真实本地 MySQL 验证租户隔离、未转换查询、已关联去重、字面量模糊匹配及分页。
- [x] 完成当前修改的类型、平台构建和可见页面检查。
- [ ] 真机顶部滚动/回前台验收（当前未连接设备）。

## 边界

渠道记录只有“全部”下展示，不伪装成待分配任务；查询不修改转换状态或历史订单。
新 API 与现有 staff-page 使用相同 rental:schedule:query 权限，关闭敏感请求/响应日志。
未生成内部单的记录不返回内部订单 ID，不使用渠道计价件数作设备数量。
不新增日期推断规则；备注缺收货日期等问题继续由既有权威策略判定。
现有管理后台历史重评可用于后续审核补建，本次没有执行生产回填。

## 2026-09-23 本地验证

- 后端 61 项通过：订单同步 12、备注日期解析 21、重评 23、本地真实 MySQL 5；无跳过。
- 新增 MySQL 用例：未转换订单按电话片段可查、租户隔离、已有内部单去重、字面量百分号、分页、号码不一致不误匹配。
- 新增同步回归：相同渠道快照仍重评 REVIEW_REQUIRED；既有备注改变但 update_time 未变的用例通过。
- 前端 test-order-search、test-staff-workflow、test-mobile-workbench、test-staff-theme 均通过。
- 目标 ESLint、vue-tsc、H5/App/微信小程序构建通过。保留既有 carbon-warehouse 图标及 Browserslist 过期警告，未升级依赖。
- 原生 Chrome Computer Use：实际 H5 构建 + 本地模拟接口，确认渠道记录可见、阻塞原因及不匹配号码空结果文案。
  截图为 outputs/staff-order-search-20260923/{channel-visible,empty-result}.png，不含真实客户信息。
- 日志：/tmp/jzd-search-{backend,lint,type,h5,app,mp}.log。
- 当前 adb 未连接设备，不能据浏览器截图宣称状态栏真机问题已验收。

## 交付状态

本次用户已授权按功能分批提交、合入 main 并推送，以及发布 1.1.1（111）APK。
本节为发布前验证记录，最终部署/公网验证结果保存在本地 outputs/jiezuda-release-1.1.1/verification.json。
既有 1.1.0 线上版本不包含本次修复。后续先上线兼容新增接口，再发新版 APK、下载页及 release.json；
公网校验通过后再报告手机版本发布完成，不覆盖旧版本号的安装包。
本次未执行历史批量补建；沿用已有管理后台重评能力，需核对范围后再执行生产数据操作。

## 线上历史候选只读核对

2026-09-23 对捷租达租户只读汇总：渠道已付款状态、金额非负、未完成退款/关闭，
排除 CONFIG_SKIPPED/CLOSED，并且没有有效内部订单关联的候选共 1,697 笔。
其中缺备注 1,078、缺收货日 188、缺发回日 53、未识别租期 21、日期顺序冲突 3、
人工复核但无现行原因字段 352、其他待处理且原因未写入 2。
这是历史候选范围，不是确认应补建的错误数量；未核对商品规则与各单历史，不允许据此自动放行。
后续应使用已有历史重评预演，审核创建/跳过/冲突/待资料分类，再决定实际回填范围。
本次查询事务为 READ ONLY，没有修改生产订单或触发真实店铺接口。
