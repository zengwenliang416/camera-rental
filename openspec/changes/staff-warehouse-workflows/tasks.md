# 员工手机仓务流程完善

基线：94f8f4a4 / 捷租达 1.0.6。用户授权实现下列功能；保留现有红色主题。
不得在测试中执行真实店铺发货、回仓或盘点调整。生产部署与数据操作单独核对。

## 交付清单

- [x] 多设备同订单同运单发货：连续扫描、去重、数量/型号/分配校验、服务端一次渠道发货。
- [x] 收货与检测分离：批量连续收货、待检测锁、复检维修、历史记录保护。
- [x] 发货结果核对：服务端查询、成功/未知/需复核区分、前后台轮询取消。
- [x] 服务端异常记录：关联业务、权限和租户隔离、负责人、处理状态、人工处理记录。
- [x] 检测配件清单及照片：按设备/型号展示，服务端验证归属，结构化记录。
- [x] 今日作业：服务端日期/队列筛选、计数和直接处理入口。
- [x] 排期增强：日期窗口可用性、周转提醒、可读时间轴。
- [x] 扫码盘点/仓位：持久化盘点单、预期/实盘/差异、核实后调整，不自动改库存。
- [x] 维修流程：待维修、处理记录、复检恢复可租。
- [x] 扫码诊断：连接/读取/请求分层，版本与脱敏诊断信息。
- [x] 相关后端行为测试、前端类型/目标 lint/平台构建、diff review。

## 已确认边界

官方发货文档 api-93586386.md 于本次读取：POST /api/open/order/ship，appid/sign/timestamp；
order_no、waybill_no、express_code、express_name 必填，成功 code=0。未提供设备列表或分批数量。
沿用集成客户端签名与请求，内部多设备关联同一运单；不虚构渠道部分发货能力。
兼容已有单设备 API 调用。金额、日期、实际设备数量仍由后端判定。

## 验收证据

本清单勾选表示本地实现及相应代码检查已完成，不表示已部署或真机验收。
详细命令、结果及现场限制见 verification.md。

## 代码交付、发布与现场验证

- [x] 按后端与手机端分批提交，并合入 main；远端推送结果以最终 Git 核验为准。
- [ ] 确认生产后端部署成功，新增表迁移执行完成。
- [ ] 发布 1.1.0 下载页与升级提醒元数据。
- [ ] 连接真机，验证光学扫码、相机上传、连续收货及弱网恢复。

## 窄屏布局补充

- [x] 工作台入口三列对齐；检测、异常、盘点操作等宽分组，主提交独立。
- [x] 今日作业五个短标签同屏显示，完整队列名称在下方展示。
- [x] 移除新增页面对 uni 对象的别名调用，使用可被跨端编译器识别的显式 API 包装。
