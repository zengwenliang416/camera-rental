# 2026-01 合并计划与进度

## 月度结论

2026-01 主要关注 dev-tools 输出目录选择和 router 小修。整体改动少，本轮已按功能点完成提取。

## 当前进度

状态：已完成，完成时间为 2026-06-07。

本轮实际改动文件：

- `scripts/open-dev-tools.js`
- `vite.config.ts`
- `src/router/interceptor.ts`

本轮未改动 `package.json` 版本号，也未吸收 router 注释清理类噪音。

### 最终补录

2026-06-07 复查 `uni-run/unibest` 后补录 `717ebbdf`。该提交属于 2026-01，之前按截图主线审计时遗漏。

- `717ebbdf`：完全合并，`src/typings.d.ts` 中 `definePage` 的 `layout` 类型从 `'default'` 扩展为 `'default' | false`，用于支持禁用布局。

补录验证：

- `git diff --check`：通过。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false`：全量仍只报 `node_modules/@wot-ui` 历史类型噪声；按诊断首行过滤后无本仓库路径输出。

## commit 分类

| 类型 | commit | 进度 | 处理结果 |
| --- | --- | --- | --- |
| 完全合并 | `c342a3f` | 已合并 | 开发者工具按 `mode` 选择 `dist/dev/${UNI_PLATFORM}` 或 `dist/build/${UNI_PLATFORM}`，`vite.config.ts` 改为 `openDevTools({ mode })`。 |
| 完全合并 | `717ebbdf` | 已补录 | `definePage` 的 `layout` 类型支持 `false`。 |
| 部分合并 | `e8ae13d` | 已合并 | 将路由不存在判断从 `page.path !== path` 修为 `page.path === path`；同时适配当前项目插件页面逻辑，让 `plugin://` 不参与普通页面校验。 |
| 忽略 | `f239744` | 已忽略 | 上游是 router 注释/逻辑清理，当前项目仍需要 `getAllPages` 做登录白名单初始化，不直接吸收。 |
| 忽略 | `01fc19a`、`b111eb8` | 已忽略 | 上游版本号更新，不覆盖当前项目版本。 |

## 代码提取点

### `c342a3f`

- `scripts/open-dev-tools.js`
  - 根据构建模式选择 `dist/dev/${UNI_PLATFORM}` 或 `dist/build/${UNI_PLATFORM}`。
- `vite.config.ts`
  - 调用 `openDevTools({ mode })`。

### `e8ae13d`

- `src/router/interceptor.ts`
  - 修正路由不存在判断方向。
  - 当前项目额外保留 404 跳转：`uni.navigateTo({ url: NOT_FOUND_PAGE })`。
  - 当前项目额外先处理 `plugin://` 页面，避免插件页被普通页面校验误判为不存在。

### `f239744`

- 不合并。
- 当前项目 `getAllPages` 不只用于路由存在判断，还用于 `judgeIsExcludePath` 动态补充登录白名单。
- 上游注释清理不适合直接套到当前项目。

## 验证要求

- `pnpm dev:mp-weixin` 后打开 `dist/dev/mp-weixin`。
- `pnpm build:mp:prod` 后如需打开，应指向 `dist/build/mp-weixin`。
- H5 不受影响。

## 验证结果

- `git diff --check -- scripts/open-dev-tools.js vite.config.ts src/router/interceptor.ts docs/merge/2026-01.md docs/merge/README.md`：通过。
- 静态断言：已确认 router 路由存在判断使用 `page.path === path`，`plugin://` 路径跳过普通页面校验，devtools 路径按 `dev/build` 分支选择，`vite.config.ts` 传入 `mode`。
- `pnpm exec eslint scripts/open-dev-tools.js vite.config.ts src/router/interceptor.ts`：失败，原因是项目既有 `eslint-plugin-jsdoc` 初始化错误：`TypeError [ERR_INVALID_ARG_TYPE]: The "path" argument must be of type string. Received undefined`。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false`：过滤 `node_modules` 后没有本仓库新增错误输出。
- `pnpm build:h5`：通过。
- `pnpm build:mp-weixin`：通过，输出提示导入 `dist/build/mp-weixin`。
- `pnpm dev:mp-weixin`：通过，输出提示导入 `dist/dev/mp-weixin`；验证后已停止该 watcher。
- H5 dev server：`http://localhost:9000/` 返回 `200 OK`。
- 最终补录验证：`layout?: 'default' | false` 已纳入本轮全量 `vue-tsc`，无本仓库新增类型错误。

## 建议提交信息

```text
fix: 合并 unibest 2026-01 devtools 与路由修复

完全合并：
- c342a3f fix(dev-tools): 根据构建模式选择正确的开发者工具路径
  - scripts/open-dev-tools.js 根据 mode 选择 dist/dev 或 dist/build
  - vite.config.ts 调用 openDevTools({ mode })

部分合并：
- e8ae13d fix(router): 修复路由不存在判断逻辑错误
  - 将 getAllPages().some(page => page.path !== path) 修为 page.path === path
  - 保留当前项目 404 跳转逻辑
  - 额外适配 plugin:// 页面，避免插件页被普通页面校验误判

忽略：
- f239744：router 注释清理不适合当前项目，getAllPages 仍用于登录白名单初始化
- 01fc19a、b111eb8：上游版本号更新，不覆盖当前项目版本

验证：
- git diff --check 通过
- pnpm build:h5 通过
- pnpm build:mp-weixin 通过，指向 dist/build/mp-weixin
- pnpm dev:mp-weixin 通过，指向 dist/dev/mp-weixin
```
