# 2026-05 合并进度

## 月度结论

2026-05 分两轮做，均已推进：

- 第一轮已合并低风险项：微信小程序 baseUrl env 化、微信开发者工具 CLI path、import-sort 关闭、`tabBar` fallback、native plugin copy 日志。
- 第二轮已部分合并 tabbar 同步修复：只提取运行时同步逻辑，不引入角色 tabbar、`TabbarItem.vue` 拆分和 CLI 模板改动。

## 时间顺序清单

`uni-run/unibest` 的 2026-05 主线存在“功能提交 + Merge branch 'base'”交错结构。下表按提交日期保留全部相关提交，实施时按功能点合并，避免漏掉真实源提交。

| 日期 | commit | 上游标题 | 分类 | 本轮处理 |
| --- | --- | --- | --- | --- |
| 2026-05-18 | `03e115f6` | `fix(tabbar): 修复并优化 tabbar 选中索引同步逻辑` | 部分合并 | 已提取路径归一化、`onShow` 同步和 router 允许路由后同步思路。 |
| 2026-05-18 | `f8cb3252` | `fix(tabbar): 修复并优化 tabbar 选中索引同步逻辑` | 部分合并 | 已提取 runtime tabbar 同步逻辑，按当前项目结构适配。 |
| 2026-05-19 | `1678b202` | `feat(cli/add): 新增 --install 参数并支持逗号分隔的 feature 输入` | 忽略 | CLI 功能，当前 app runtime 不需要。 |
| 2026-05-19 | `60f8b187` | `docs: clarify main and base branch workflow` | 忽略 | 上游分支工作流文档，不适合合入当前项目。 |
| 2026-05-19 | `1c1c7bd3` | `fix: 优化 tabbar 同步并移除 darwin 依赖` | 部分合并 | 已提取异步同步、H5 可见性恢复同步、点击后同步。 |
| 2026-05-19 | `eab2e808` | `Merge branch 'base'` | 部分合并 | 对应 tabbar 同步的主线 merge 结点。 |
| 2026-05-19 | `d3a411d8` | `fix: 支持配置微信开发者工具路径` | 完全合并 | `scripts/open-dev-tools.js` 支持自定义微信 CLI 路径。 |
| 2026-05-19 | `278610af` | `Merge branch 'base'` | 完全合并 | 对应 `d3a411d8` 的主线 merge 结点。 |
| 2026-05-20 | `bbf56546` | `fix: 修复 tabbar 选中状态错位` | 部分合并 | 已提取导航成功后同步、导航失败回滚；不保留已被上游回滚的页面栈 fallback。 |
| 2026-05-20 | `ce8ed973` | `Merge branch 'base'` | 部分合并 | 对应 tabbar 状态错位修复的主线 merge 结点。 |
| 2026-05-20 | `36595585` | `fix: 支持在 env 配置微信开发者工具路径` | 完全合并 | `env/.env` 增加示例；`vite.config.ts` 通过 `loadEnv(..., '')` 传入非 `VITE_` 变量。 |
| 2026-05-20 | `d1898f21` | `Merge branch 'base'` | 完全合并 | 对应 `36595585` 的主线 merge 结点。 |
| 2026-05-20 | `63aff76d` | `docs(env): 更新.env中微信开发者工具CLI路径的注释说明` | 部分合并 | 保留说明意图，采用本仓库更短的中文注释。 |
| 2026-05-20 | `8d17bd89` | `fix: 禁用 import 自动排序` | 完全合并 | 关闭 `perfectionist/sort-imports`，避免破坏 uni 条件编译 import 边界。 |
| 2026-05-20 | `081a20ca` | `Merge branch 'base'` | 完全合并 | 对应 `8d17bd89` 的主线 merge 结点。 |
| 2026-05-20 | `0240ca4b` | `fix: 默认复制 App 原生插件资源` | 部分合并 | 只提取复制成功日志，不拿默认开启。 |
| 2026-05-20 | `4024e1fa` | `Merge branch 'base'` | 部分合并 | 对应 `0240ca4b` 的主线 merge 结点。 |
| 2026-05-21 | `78c67dce` | `fix: 修复微信小程序请求地址覆盖` | 部分合并 | 提取微信小程序 env baseUrl 逻辑，保留当前默认后端地址。 |
| 2026-05-21 | `1c6a48de` | `Merge branch 'base'` | 部分合并 | 对应 `78c67dce` 的主线 merge 结点。 |
| 2026-05-22 | `26e6900b` | `fix: avoid undefined tabBar config` | 完全合并 | `tabBar` fallback 从 `undefined` 改成 `{}`。 |
| 2026-05-22 | `1a1e2531` | `Merge branch 'base'` | 完全合并 | 对应 `26e6900b` 的主线 merge 结点。 |

## 已合并代码

### 微信小程序 baseUrl env 化

对应：`78c67dce`、`1c6a48de`。

- `env/.env`
  - 保留当前默认 `VITE_SERVER_BASEURL = 'http://localhost:48080/admin-api'`。
  - 增加 `VITE_SERVER_BASEURL__WEIXIN_DEVELOP`、`VITE_SERVER_BASEURL__WEIXIN_TRIAL`、`VITE_SERVER_BASEURL__WEIXIN_RELEASE` 注释示例。
- `env/.env.development`、`env/.env.production`、`env/.env.test`
  - 同步上游 mode 注释，明确对应环境文件。
- `src/env.d.ts`
  - 增加三个可选微信小程序专用地址类型。
- `src/utils/index.ts`
  - 删除原本硬编码的微信小程序 develop / trial / release 地址。
  - 改为按 `uni.getAccountInfoSync().miniProgram.envVersion` 从 `import.meta.env` 读取。
  - 未配置专用地址时回退 `VITE_SERVER_BASEURL`。
- `vite.config.ts`
  - 合并上游关于非 H5 端 dev 命令仍是 build command 的注释。

决策：只改配置来源，不改当前默认地址，避免影响本地和已有后端联调。

### 微信开发者工具 CLI path

对应：`d3a411d8`、`278610af`、`36595585`、`d1898f21`、`63aff76d`。

- `env/.env`
  - 增加 `WECHAT_DEVTOOLS_CLI_PATH` macOS / Windows 注释示例。
  - 使用非 `VITE_` 前缀，避免暴露给客户端。
- `vite.config.ts`
  - 增加 `envDir`。
  - 保留 `loadEnv(mode, envDir)` 读取客户端 `VITE_` 变量。
  - 增加 `loadEnv(mode, envDir, '')` 读取 `WECHAT_DEVTOOLS_CLI_PATH`。
  - 调用 `openDevTools({ mode, wechatDevtoolsCliPath: WECHAT_DEVTOOLS_CLI_PATH })`。
- `scripts/open-dev-tools.js`
  - `_openDevTools(env, options)` 支持 `wechatDevtoolsCliPath`。
  - macOS / Windows 微信小程序优先使用配置路径，未配置时回退默认路径。
  - 打开失败时打印当前 CLI 命令和配置提示。

决策：不从 `process.env.WECHAT_DEVTOOLS_CLI_PATH` 直接读，统一由 Vite `loadEnv` 传入，和上游最终状态一致。

### import 自动排序

对应：`8d17bd89`、`081a20ca`。

- `eslint.config.mjs`
  - 关闭 `perfectionist/sort-imports`。

原因：uni 条件编译注释可能包裹 import，自动排序会把 import 移出平台条件边界。

### `tabBar` fallback

对应：`26e6900b`、`1a1e2531`。

- `src/tabbar/config.ts`
  - `export const tabBar = tabbarCacheEnable ? _tabbar : {}`。

原因：`NO_TABBAR` 或无缓存 tabbar 场景下，避免 `pages.config.ts` 拿到 `undefined`。

### native plugin copy 日志

对应：`0240ca4b`、`4024e1fa`。

- `vite-plugins/copy-native-resources.ts`
  - `verbose` 开启时，复制成功日志输出 `sourcePath -> targetPath`。
  - `verbose` 开启时，保留复制文件/目录数量日志。
- `env/.env`
  - 保持 `VITE_COPY_NATIVE_RES_ENABLE = false`，不合并上游默认开启。
- `vite.config.ts`
  - 保持 `UNI_PLATFORM === 'app' && VITE_COPY_NATIVE_RES_ENABLE === 'true'`。

决策：只拿日志可观测性，不改变 App 构建默认行为，也不改变原有 `verbose` 控制。

### tabbar 同步专项

对应：`03e115f6`、`f8cb3252`、`1c1c7bd3`、`eab2e808`、`bbf56546`、`ce8ed973`。

已合并内容：

- `src/tabbar/store.ts`
  - 新增 `normalizeRoutePath(path?: string)`。
  - `isPageTabbar()` 使用 normalize 后路径，并把 `/` 视为首页 tabbar。
  - 新增 `findTabbarIndexByPath()`。
  - `setAutoCurIdx()` 找到 tabbar 路径时同步高亮；找不到时只在当前 index 越界时回退首页。
  - 新增 `syncCurIdxByCurrentPage()` / `syncCurIdxByCurrentPageAsync()`。
  - 新增 `isCurrentRouteTabbarItem(index)`。
  - `setCurIdx()` 继续保留当前项目的登录态、白名单/黑名单 guard。
- `src/tabbar/index.vue`
  - 点击当前 tab 时，只有“当前高亮且当前路由就是该 tab”才阻止跳转。
  - `switchTab` / `navigateTo` 成功后异步同步当前路由。
  - 跳转失败时回滚到点击前的 tabbar index。
- `src/App.vue`、`src/App.ku.vue`
  - `onShow` 后同步当前 tabbar index。
  - H5 下监听 `visibilitychange` / `pageshow` 后同步 tabbar。
- `src/router/interceptor.ts`
  - 新增 `allowRoute(path)`，只在路由允许通过时同步 tabbar index。
  - 未登录或黑名单拦截时不提前切换 tabbar 高亮。

适配决策：

- 不引入上游角色 tabbar；当前项目仍使用 `reactive` 数组，不使用上游 `tabbarList.value` 写法。
- 不拆分 `TabbarItem.vue`；当前项目继续保留单文件 tabbar 实现。
- 不保留 `1c1c7bd3` 中“从页面栈倒序寻找最近 tabbar 页”的 fallback；该逻辑已被上游 `bbf56546` 删除，用于修复 tabbar 选中状态错位。
- `03e115f6` 的 router interceptor 改动在上游 CLI 模板中，本轮只把相同的“允许路由后再同步”思路适配到当前 runtime router。

2026-06-07 最终补录调整：

- 角色 tabbar 已作为 2026-02 漏审补录单独实现。
- 本地实现只增加 `roles?: string[]` 可见性机制，保留当前 `reactive` 数组、原始 index、登录态 guard 和白名单/黑名单 guard，不改成上游 `computed` 列表。

## 暂不合并

### CLI / 上游文档

- `1678b202`：CLI add 参数，当前 app runtime 不需要。
- `60f8b187`：上游分支工作流文档，不合入当前项目。
- `7410a59`：H5 导航守卫不合并。上游依赖 `$router.beforeEach` 并删除 `onShow` 同步；当前项目源码仍走 uni-app 原生路由和 `uni.addInterceptor`，已有 `allowRoute + onShow + visibilitychange/pageshow` 覆盖同步。
- `f05992e`：上游模板版本号，不覆盖当前项目版本体系。

## 验证记录

### 低风险配置提交

已跑：

- `git diff --check -- <本轮文件>`：通过。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false`：全量仍只报 `node_modules/@wot-ui` 历史类型噪声。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false 2>&1 | awk ...`：过滤依赖错误后无本仓库输出。
- `pnpm exec eslint <本轮触达文件>`：仍失败在既有 `eslint-plugin-jsdoc` 初始化错误，错误为 `The "path" argument must be of type string. Received undefined`。
- `pnpm build:h5`：通过。
- `SKIP_OPEN_DEVTOOLS=true pnpm build:mp:prod`：通过。

未做手测，后续可补：

- 微信小程序 develop / trial / release 三种 `envVersion` 的 baseUrl fallback。
- 配置 `WECHAT_DEVTOOLS_CLI_PATH` 后确认失败日志打印配置后的 CLI 命令。
- `NO_TABBAR` 场景下确认 `pages.config.ts` 不再产生 `undefined` tabBar。

### tabbar 同步专项

已跑：

- `git diff --check -- src/App.vue src/App.ku.vue src/router/interceptor.ts src/tabbar/index.vue src/tabbar/store.ts docs/merge/2026-05.md docs/merge/README.md`：通过。
- `pnpm exec eslint src/App.vue src/App.ku.vue src/router/interceptor.ts src/tabbar/index.vue src/tabbar/store.ts`：仍失败在既有 `eslint-plugin-jsdoc` 初始化错误。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false`：全量仍只报 `node_modules/@wot-ui` 历史类型噪声。
- `pnpm exec vue-tsc --noEmit --skipLibCheck --pretty false 2>&1 | awk ...`：过滤依赖错误后无本仓库输出。
- `pnpm build:h5`：通过。
- `SKIP_OPEN_DEVTOOLS=true pnpm build:mp:prod`：通过。

H5 手测：

- 登录后工作台、审批、通讯录、消息、我的之间连续切换，URL 和页面内容正确。
- 从工作台进入非 tabbar 页“用户管理”时底部 tabbar 隐藏；返回后回到工作台并恢复 tabbar。
- 未登录直达 `/pages/message/index` 会先跳登录页，登录后回到消息页。
- 手测期间 console 有 1 条既有 `z-paging-complete-error`，发生在未登录直达消息页被重定向期间，不是 tabbar runtime 异常。

未做：

- H5 浏览器 tab 切走再切回的可见性同步。
- 微信开发者工具内手动验证 `switchTab` 和分享/直达路径。

### 最终补录复核

- `7410a59` 未合并：本项目没有源码层面的 `$router.beforeEach` 使用，新增 H5 router hook 大概率是 no-op；当前保留 `allowRoute + onShow + visibilitychange/pageshow`。
- `f05992e` 未合并：仅上游模板版本号。
- 最终补录后再次运行 `pnpm build:h5`、`SKIP_OPEN_DEVTOOLS=true pnpm build:mp:prod`、`SKIP_OPEN_DEVTOOLS=true pnpm build:mp-toutiao` 均通过。

## 建议提交信息

```text
fix: 合并 unibest 2026-05 tabbar 同步修复

部分合并：
- 03e115f6 / f8cb3252【部分合并】修复并优化 tabbar 选中索引同步逻辑
  - src/tabbar/store.ts 新增 normalizeRoutePath、syncCurIdxByCurrentPage、syncCurIdxByCurrentPageAsync
  - isPageTabbar 使用归一化路径，并把 / 视为首页 tabbar
  - App.vue / App.ku.vue 在 onShow 后同步当前 tabbar index
  - src/router/interceptor.ts 改为只在路由允许通过时同步 tabbar index
- 1c1c7bd3 / eab2e808【部分合并】优化 tabbar 同步
  - H5 监听 visibilitychange / pageshow 后异步同步 tabbar
  - 点击当前高亮且当前路由就是该 tab 时才阻止重复跳转
  - switchTab / navigateTo 成功后异步同步当前路由
- bbf56546 / ce8ed973【部分合并】修复 tabbar 选中状态错位
  - tabbar 跳转前记录 prevIdx
  - 导航失败时回滚到点击前的 tabbar index
  - 不保留已被上游删除的页面栈 fallback

本地适配：
- 保留当前项目 reactive tabbarList，不引入上游角色 tabbar 的 computed 结构
- 保留 setCurIdx 中当前项目登录态、白名单/黑名单 guard
- 不拆分 TabbarItem.vue，不引入上游 CLI 模板改动

忽略：
- 1678b202【忽略】CLI add 参数
- 60f8b187【忽略】上游分支工作流文档

验证：
- git diff --check 通过
- vue-tsc 全量仍只报 node_modules/@wot-ui 历史类型噪声，过滤依赖错误后无本仓库输出
- pnpm build:h5 通过
- SKIP_OPEN_DEVTOOLS=true pnpm build:mp:prod 通过
- H5 已手测五个 tabbar 入口和非 tabbar 页返回
- ESLint 仍失败在项目既有 eslint-plugin-jsdoc 初始化错误
```
