# Camera Rental Platform — Codex Instructions

## 1. 项目目标

本工作区用于开发相机及摄影器材租赁平台。

系统必须支持：

- Web 管理后台
- PC Web 租赁商城
- uni-app 客户端，包括微信小程序、H5 和 App
- 员工移动端，包括扫码出库、归还、检测和维修
- 闲鱼订单同步（闲管家开放平台）
- 相机、镜头、无人机和配件的进销存
- 单台设备级别的排期、分配、维修和收益统计

这是一个多仓库工作区。修改代码前，先确认需求涉及哪些仓库，不要默认所有功能都写在同一个项目中。

## 2. 仓库结构

```text
camera-rental/
├── camera-rental-server/     # Java 后端，ruoyi-vue-pro（master-jdk17）
├── camera-rental-admin/      # Web 管理后台，Vue 3 + Element Plus
├── camera-rental-uniapp/     # 客户端，uni-app
├── camera-rental-staff/      # 员工移动端，uni-app
├── camera-rental-web/        # PC 客户官网，Nuxt
├── docs/                     # 跨仓库架构和业务文档
└── AGENTS.md
```

各仓库职责：

- `camera-rental-server`：领域规则、数据库、权限、接口、支付、排期和第三方同步。
- `camera-rental-admin`：订单、设备、排期、仓库、采购、维修和经营报表。
- `camera-rental-uniapp`：客户浏览商品、选择租期、下单、支付和查看订单。
- `camera-rental-staff`：员工扫码、出库、回仓、检测和维修登记。
- `camera-rental-web`：PC 客户官网、商品展示、租赁下单和 SEO。

前端不得自行实现权威库存、排期或金额计算。业务规则必须以后端结果为准。

### 2.1 当前基线

| 仓库 | 上游 | 当前分支 / 基线 | 备注 |
|---|---|---|---|
| server | YunaiV/ruoyi-vue-pro | `master-jdk17` | JDK 17/21，Spring Boot 3 |
| admin | yudaocode/yudao-ui-admin-vue3 | `master` | Vue3 + Element Plus，pnpm |
| uniapp | yudaocode/yudao-mall-uniapp | `master` | Vue3 + uni-app |
| staff | yudaocode/yudao-ui-admin-uniapp | `master` | 员工端，pnpm |
| web | 自建 | `main` | Nuxt，当前使用 bun |

## 3. 开始任务前

开始修改前必须：

1. 阅读当前目录以及更深层目录中的 `AGENTS.md`。
2. 阅读目标仓库的 `README.md`、`package.json`、锁文件或 `pom.xml`。
3. 检查现有代码模式，不要凭经验猜测目录、命令和类型。
4. 明确此次修改涉及的仓库、接口、数据库表和业务规则。
5. 检查工作区现有未提交修改，不得覆盖用户的其他改动。
6. 优先做最小范围修改，不进行与需求无关的大规模重构。
7. 若任务涉及闲管家 / 闲鱼，先读 `docs/integrations/xianyu/source.md` 与在线接口索引。

跨仓库功能应先确定后端 API 和数据结构，再修改对应前端。

## 4. 后端架构

后端使用：

- JDK 17
- Spring Boot 3
- Maven
- MyBatis Plus
- MySQL
- Redis
- ruoyi-vue-pro 单体架构

相机租赁业务统一放在：

```text
yudao-module-rental
```

建议结构：

```text
yudao-module-rental/
├── yudao-module-rental-api/
└── yudao-module-rental-biz/
    └── src/main/java/.../module/rental/
        ├── controller/admin/
        ├── controller/app/
        ├── service/
        ├── dal/
        ├── convert/
        ├── enums/
        ├── integration/xianyu/
        └── job/
```

接口约定：

```text
/admin-api/rental/**
```

供管理后台和员工端使用。

```text
/app-api/rental/**
```

供客户 Web 和 uni-app 使用。

优先复用已有模块（按需在 `pom.xml` 启用）：

- `yudao-module-system`：员工、角色、菜单和权限
- `yudao-module-member`：客户会员
- `yudao-module-pay`：支付和退款
- `yudao-module-mall`：商品展示和营销基础能力
- `yudao-module-erp`：采购、供应商和基础库存
- `yudao-module-infra`：文件、任务、配置和日志

不要把租赁业务直接硬塞进商城订单表。租赁订单、设备排期和设备分配应使用独立领域表，通过映射字段关联商城、闲鱼和支付订单。

## 5. 核心领域模型

至少区分以下概念：

- 商品 SKU：例如“索尼 A7M4”。
- 设备实例：例如“A7M4-0001”，代表具体的一台设备。
- 租赁订单：客户与费用信息。
- 租赁明细：订单中的商品、数量和租期。
- 设备分配：具体设备实例与订单明细的关联。
- 计租周期：客户需要支付租金的日期范围。
- 设备占用周期：设备从出库到回仓检测完成的日期范围。
- 渠道订单：闲鱼、Web、小程序等外部订单映射。

推荐使用独立数据表：

```text
rental_product_config
rental_device
rental_package
rental_package_item
rental_order
rental_order_item
rental_schedule
rental_device_assignment
rental_delivery
rental_inspection
rental_maintenance
rental_deposit
rental_channel_order
```

不要用商品库存数量代替单台设备管理。

## 6. 排期规则

排期冲突必须以“设备占用周期”为准，不能只看计租周期。

示例：

```text
发货：7 月 22 日
客户收货：7 月 24 日
计租开始：7 月 25 日
计租结束：7 月 27 日
客户发回：7 月 27 日
设备回仓：7 月 29 日
检测完成：7 月 30 日
```

对应：

```text
计租周期：7 月 25 日至 7 月 27 日
设备占用周期：7 月 22 日至 7 月 30 日
```

必须满足：

- 同一设备实例不能存在重叠的有效占用记录。
- 排期检查和设备分配必须在事务中完成。
- 并发分配必须有数据库约束、锁或其他可靠的并发控制。
- 取消、退款、换机、续租和提前归还必须同步调整排期。
- 不能仅靠前端检查是否有库存。

## 7. 闲鱼订单与闲管家规则（摘要）

完整规范见：

- `docs/integrations/xianyu/source.md`
- `docs/domain/xianyu-integration.md`
- 在线索引：`https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/llms.txt`

业务口径：

- `pay_amount` 是闲鱼订单实付金额，作为该订单的总租金收入。
- 没有备注或备注解析失败的订单，仍然必须计入总租金。
- `seller_remark` 主要用于解析发货、收货、计租和发回日期。
- 退款金额单独统计，不要未经确认直接从租金中扣除。
- 同时保存原始接口数据和标准化数据，便于审计和重新解析。
- 同步必须使用外部订单号实现幂等，不能重复创建订单。
- 不得在日志中输出 AppSecret、完整手机号、地址或身份证信息。

备注日期规则：

1. 存在明确的 `#租期7.25-7.27#` 时，优先使用明确租期。
2. 没有明确租期，但有“收货”和“发回”时：
   - 计租开始日期等于收货日期加一天。
   - 发回当天计租。
3. 发货日期不参与计租天数。
4. 缺少年份时，结合订单时间推断。
5. 日期冲突或关键信息不足时，标记为人工复核，不得静默猜测。

每日租金分摊必须精确到分，所有日期分摊金额之和必须严格等于订单实付金额。无法整除的分币余数放到最后一个计租日。

## 8. 金额和时间

- 后端金额遵循项目现有规范，优先使用整数“分”存储。
- Java、JavaScript 和数据库中不得使用浮点数进行金额结算。
- 前端只负责把分转换成元进行展示。
- 所有业务日期统一按 `Asia/Shanghai` 理解。
- 租期和排期使用日期类型表达，不要用字符串比较日期。
- 接口时间格式必须遵循现有 ruoyi-vue-pro 规范。
- 跨月、跨年和闰年必须由日期库处理，不得手写天数计算。

## 9. API 规范

后端接口应遵循现有芋道项目约定：

- 使用统一的返回结构。
- 管理端和客户端 Controller 分开。
- 分页接口使用统一分页对象。
- Controller 不直接访问 Mapper。
- 数据库实体不得直接作为接口响应。
- 使用 Request VO、Response VO 和转换层。
- 参数校验放在请求对象和 Service 入口。
- 权限校验不能只依赖前端菜单。
- 新增或修改 API 时，同步修改所有受影响前端。
- 破坏性接口变更必须先说明迁移方案。

## 10. 数据库规则

- 数据库结构变更必须提供增量 SQL。
- 不要修改已经在其他环境执行过的历史迁移文件。
- 外部订单号、设备编号和序列号应建立适当的唯一约束。
- 排期、订单状态、店铺、商品和时间范围查询应建立必要索引。
- 删除业务数据时优先遵循项目现有逻辑删除规范。
- 不得通过删除历史订单来修复同步问题。
- 渠道原始数据应保留可追踪性。
- 涉及订单、支付、库存和排期的写操作必须明确事务边界。

## 11. Web 管理后台

`camera-rental-admin` 使用 Vue 3、TypeScript、Element Plus 和项目现有组件体系。

要求：

- 优先复用已有 API、表格、表单、弹窗、字典和权限组件。
- 不要引入另一套 UI 框架。
- 避免无理由使用 `any`。
- API 类型与后端 VO 保持一致。
- 页面必须处理加载、空数据、错误和无权限状态。
- 排期页面必须清楚区分计租周期和设备占用周期。
- 金额、日期、状态的格式化逻辑应集中复用。
- 涉及设备或订单详情的页面应避免展示不必要的敏感信息。
- 包管理器使用 `pnpm`，不得混用 npm / yarn。

## 12. uni-app 客户端

`camera-rental-uniapp` 使用 Vue 3 和 uni-app。

要求：

- 页面必须兼容微信小程序、H5 和 App。
- 不得依赖只在浏览器存在的 DOM API。
- 平台差异使用 uni-app 官方能力和条件编译处理。
- 不要在客户端保存支付密钥或闲鱼接口密钥。
- 下单前的库存结果只能作为展示，最终排期必须由后端再次确认。
- 租金计算以后端返回值为准。
- 登录失效、支付取消、库存冲突和重复提交必须有明确提示。
- 图片、设备列表和订单列表应考虑移动网络和分页加载。

## 13. 员工移动端

`camera-rental-staff` 主要服务仓库和运营人员。

重点功能：

- 扫码识别具体设备实例
- 发货拣货
- 出库复核
- 回仓登记
- 设备检测
- 损坏记录
- 维修登记
- 排期查询

扫码操作必须由后端校验设备状态，不得仅修改前端状态。包管理器使用 `pnpm`。

## 14. PC Web 官网

`camera-rental-web` 使用 Nuxt 时：

- 页面必须兼容 SSR。
- 不得在客户端代码中暴露服务端密钥。
- 商品详情、分类和品牌页面应支持 SEO。
- Web 与 uni-app 共用后端业务接口，不得复制一套独立订单逻辑。
- 服务端渲染期间不得直接使用 `window`、`document` 或浏览器存储。
- 登录态、支付回调和订单状态以后端为准。
- 当前仓库已有 `bun.lock`，优先沿用现有包管理器；不要无故切换。

## 15. 依赖和配置

- 优先使用仓库当前包管理器和锁文件。
- 前端仓库 `admin` / `staff` 使用 `pnpm`。
- 没有必要时不要新增生产依赖。
- 新增依赖前检查现有依赖是否已经提供相同能力。
- 不得提交真实密钥、密码、Token、AppSecret、证书或生产地址。
- 本地配置使用环境变量或不提交的配置文件。
- 提供 `.env.example` 时只能放占位值。

## 16. 测试和验证

修改后只报告真实执行过的检查，不得声称未运行的测试已经通过。

后端优先执行：

```bash
mvn -pl yudao-module-rental -am test
```

如果存在 Maven Wrapper，优先使用：

```bash
./mvnw -pl yudao-module-rental -am test
```

前端先检查 `package.json` 中真实存在的脚本，再按需执行。不要运行不存在的脚本。

最低验证要求：

- 后端业务修改：执行相关模块测试或编译。
- API 修改：验证请求、响应和错误场景。
- 管理后台修改：执行类型检查，并验证目标页面。
- uni-app 修改：至少验证 H5 和微信小程序构建。
- 排期修改：测试边界重叠、并发分配、取消和续租。
- 金额修改：测试分币、跨月、退款和总额守恒。
- 闲鱼同步修改：测试分页、幂等、重试和备注解析失败。
- 跨仓库修改：验证所有受影响仓库的接口一致性。

## 17. 安全与隐私

- 所有管理接口都必须验证权限。
- 多租户开启时必须遵循现有租户隔离机制。
- 支付回调和第三方回调必须验证签名并保证幂等。
- 日志不得包含密钥、完整身份证、手机号、地址或支付凭证。
- 订单号在内部可以完整保存；在普通看板、截图和对外导出中默认脱敏。
- 不得通过 Cookie、抓包或非正式方式访问闲鱼数据。
- 不得绕过后端直接从前端调用需要密钥的第三方接口。

## 18. Git 规则

除非用户明确要求：

- 不执行 `git commit`。
- 不执行 `git push`。
- 不创建 Pull Request。
- 不删除用户已有分支。
- 不使用 `git reset --hard`。
- 不覆盖与当前任务无关的未提交修改。
- 不大范围格式化未修改文件。

提交内容应保持单一目的，避免把后端、前端格式化和无关升级混在同一变更中。

各子仓库当前 `origin` 仍指向上游官方仓库。后续切到自有远程时，应保留 `upstream` 以便同步官方更新。

## 19. 文档

架构性或跨仓库规则应写入：

```text
docs/
├── architecture.md
├── domain/
│   ├── rental-order.md
│   ├── device-scheduling.md
│   └── xianyu-integration.md
├── integrations/
│   └── xianyu/
│       └── source.md
├── api/
└── decisions/
```

`AGENTS.md` 只保存稳定的工作规则和项目地图，不作为全部业务文档。

如果行为、接口或业务口径发生变化，应同步更新对应文档。

## 20. 完成任务时

最终回复必须说明：

1. 修改了哪些仓库和文件。
2. 实现了什么行为。
3. 执行了哪些测试或构建命令。
4. 哪些检查没有执行以及原因。
5. 是否存在数据库迁移、配置变更或兼容性风险。
6. 是否仍有需要人工确认的假设。

没有完成或无法验证的内容必须明确说明，不得伪装为成功。

## 21. 闲管家开放平台接口规范

闲管家是闲鱼店铺、商品、订单、售后和推送的外部渠道。完整业务规则见：

- `docs/domain/xianyu-integration.md`
- `docs/integrations/xianyu/`
- `https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/llms.txt`

### 21.1 开发前置条件

开发或修改任何闲管家功能前必须：

1. 重新读取当前在线 `llms.txt`。
2. 从索引定位本次实际使用的接口。
3. 打开目标接口完整 Markdown 文档后再写代码。
4. 记录请求地址、方法、认证 / 签名、字段、响应、错误码和限制。
5. 文档与旧代码冲突时，以当前在线文档为准，并记录兼容性影响。

不得仅凭记忆、历史聊天、旧代码或字段名称猜测接口行为。

### 21.2 集成边界

闲管家客户端统一放在后端租赁模块的：

```text
yudao-module-rental-biz/src/main/java/.../module/rental/integration/xianyu/
```

建议按 `client`、`config`、`dto`、`convert`、`webhook`、`service`、`enums` 和
`exception` 分层。业务 Service 不得直接拼签名或发送 HTTP。

### 21.3 只读与写操作

默认只实现和测试只读能力：店铺、商品、订单、售后和快递公司查询。

创建 / 编辑商品、修改库存、上下架、删除商品、订单发货、订单改价和同意 /
拒绝退款均属于写操作。执行写操作必须有用户明确授权、目标店铺二次校验、
幂等控制和审计记录，默认只允许测试店铺，自动化测试不得操作真实店铺。

### 21.4 凭据与日志

凭据只能通过环境变量、配置中心或密钥管理系统注入：

```yaml
rental:
  xianyu:
    enabled: false
    base-url: https://open.goofish.pro
    app-key: ${XGJ_APP_KEY:}
    app-secret: ${XGJ_APP_SECRET:}
    webhook-base-url: ${XGJ_WEBHOOK_BASE_URL:}
```

不得把真实 AppKey、AppSecret、签名、完整请求头、客户手机号、地址或身份证
写入 Git、前端、普通日志、测试 Fixture 或文档。日志和导出必须脱敏。

### 21.5 店铺与订单同步

应用和店铺授权必须分开建模。同步订单前先检查店铺授权状态；授权失效时停止
该店铺写操作，保留历史订单和排期，并记录同步异常。

订单同步采用“订单推送 + 定时增量同步 + 订单详情补拉”：

1. 保存未经业务处理的原始推送。
2. 使用外部订单号幂等。
3. 拉取并保存订单详情。
4. 转换为内部租赁订单并解析备注。
5. 进入排期计算或人工复核。
6. 使用安全游标补偿遗漏和状态变化。

必须固定同步边界，处理相同更新时间的多条记录，并区分接收、去重和失败数量。

### 21.6 金额与备注口径

- `pay_amount` 作为闲鱼订单实付金额和总租金收入来源。
- 备注为空或解析失败不能导致订单从总租金中消失。
- `seller_remark` 只用于提取发货、收货、计租和发回日期。
- 退款、押金退还和设备赔偿分开统计。
- 金额进入内部系统后使用整数分，日期统一按 `Asia/Shanghai` 处理。
- 明确租期优先；没有明确租期时，收货次日开始计租，发回当天计租。
- 日期冲突或信息不足时进入人工复核，不得静默猜测。

### 21.7 原始数据与测试

渠道原始 JSON、标准化字段、转换版本、同步时间和异常原因必须可追溯保存，
支持历史订单重新解析。测试默认使用脱敏 Fixture 或 Mock Server，真实接口测试
必须显式开启、默认只读且不进入普通 CI。
