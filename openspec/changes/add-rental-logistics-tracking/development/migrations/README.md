# Development Migrations: add-rental-logistics-tracking

## Execution Order

1. Back up the target database.
2. Deploy code that tolerates both the old nullable shipment shape and the new logistics tables.
3. Execute `camera-rental-server/sql/mysql/migrations/20260731_032_rental_delivery_tracking.sql`.
4. Keep Provider, query, and subscription switches disabled until task 005 configuration is verified.

## Validation

- Verify the production SQL SHA-256
  `4cb75cf9a2d8278e6a3d225af5cc24d58934716e709e6c25fdeeb2210d0ee36d`
  against `manifest.json`.
- Confirm eight logistics tables exist,
  `rental_delivery.provider_credential_id` is nullable, and
  `rental_device_shipment.delivery_id` is nullable.
- Confirm tenant-prefixed unique keys permit the same waybill, payload hash, and dedupe key in different tenants.
- Re-run the fresh-schema and current-upgrade migration paths in final six-domain E2E verification.
- Confirm no historical Delivery rows, subscription tasks, or Provider requests are generated by migration.

## Executed Evidence

On 2026-07-31, a disposable `mysql:8.4` container validated both paths:

- Fresh schema: imported `ruoyi-vue-pro.sql`, then executed migrations `001-032` in order.
- Legacy single-credential upgrade: imported `ruoyi-vue-pro.sql`, executed migrations
  `001-031`, created the pre-release Provider config shape with encrypted fixture
  values, then executed migration `032` twice.
- Both schemas contained all eight logistics tables, nullable
  `rental_delivery.provider_credential_id`, and nullable
  `rental_device_shipment.delivery_id`.
- The legacy encrypted customer code and API key were copied byte-for-byte into
  one credential named `default`; repeat execution did not create a duplicate.
- Migration execution created no runtime Delivery, Outbox, or Inbox rows.

## Rollback

Prefer rolling back the application while retaining additive tables. A destructive
rollback requires a database backup and explicit retention approval. Stop workers
first, verify no shipment references `delivery_id` and no Delivery references
`provider_credential_id`, then remove those indexes and columns before dropping
the eight logistics tables in reverse dependency order.
